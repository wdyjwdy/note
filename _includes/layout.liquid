<!doctype html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="icon" href="/src/heart.svg" type="image/svg+xml">
        <link rel="stylesheet" href="/src/style.css" />
        <title>{{ title }}</title>
        {% if mermaid %}
        <script src="/src/mermaid.min.js"></script>
        <script>mermaid.initialize({ startOnLoad: true });</script>
        {% endif %}
    </head>
    <body>
        <h1>{{ title }}</h1>
        {% if toc %}{{ content | toc }}{% endif %}
        {{ content }}
    </body>
</html>
